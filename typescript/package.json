{
  "name": "genie_api_client",
  "version": "1.0.0",
  "private": false,
  "license": "MIT",
  "homepage": "https://github.com/bitforge/genie-api-clients",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist/",
    "compile": "tsc",
    "build": "yarn clean && yarn compile",
    "test": "jest --env=node --colors tests"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "^4.4.4"
  },
  "dependencies": {}
}
